# TASK-007 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/20260328-120712-task-006-model-api-and-venv-closure.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：TASK-007
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（TASK-006）的实测审查，结论如下：

1. 可信且已复现：
- `apps/api/app/main.py` 确实挂载 `results` 路由前缀 `/api/results`
- `apps/api/app/routers/results.py` 确实存在 `GET /{experiment_id}` 与 `GET /{experiment_id}/download-model`
- `.venv` 下可执行测试，且结果与汇报一致：`26 passed, 2 failed`（Parquet 用例缺少 pyarrow）

2. 汇报与证据不一致：
- 汇报称“README 已新增 .venv 测试环境说明”，但当前 `README.md` 中未发现该新增段落（仅有 workspace 说明与现有测试命令）

3. 口径待修正：
- TASK-006 标题为“测试环境标准化”，但 `.venv` 下仍有 2 个失败用例；需要明确是“核心路径达标”还是“全量测试达标”，并在测试策略上消除歧义

因此本轮目标：修复文档证据不一致问题，并把 Parquet 测试策略与依赖策略标准化，形成稳定、可复现、口径一致的测试基线。

---

## 三、Trae 多智能体协作（建议）

- system-architect：定义测试基线分层（核心/扩展）与验收口径
- backend-expert：确认结果路由与模型读取链路文档一致性
- qa-engineer：修复 Parquet 用例策略（依赖安装或条件跳过）并输出稳定回归结果
- devops-architect：固化 .venv 依赖安装方案（最小依赖 + 可选依赖）
- tech-writer：补齐 README 与测试运行文档，确保“文档即真相”
- tech-lead-architect：统一证据口径并输出最终报告

说明：可自行增减智能体，但必须在汇报中写明分工、动作、证据与结论。

---

## 四、任务范围

### 任务 1：修复 README 与实际实现不一致（必须完成）

目标：
- 确保文档与代码、测试行为完全一致，避免“汇报已完成但仓库无证据”。

必须完成：
1. README 修复：
- 补充 `.venv` 激活与使用方式
- 补充“推荐使用 `.venv` 执行测试”说明
- 补充 `results` 相关端点说明：`/api/results/{experiment_id}` 与 `/api/results/{experiment_id}/download-model`

2. 证据校验：
- 提供 README 修改前后对照（关键段落）
- 提供命令级验证，证明文档中命令可执行

验收标准：
- README 中存在并可检索到上述段落
- 文档命令可在本地执行
- 汇报描述与仓库文件一致

---

### 任务 2：标准化 Parquet 测试策略并稳定回归结果（必须完成）

目标：
- 消除“26/28 通过但口径不清”的状态，明确核心与扩展测试边界。

必须完成（二选一并给出理由）：
1. 依赖闭环方案：
- 在 `.venv` 安装 `pyarrow`（或 fastparquet）
- 让 `tests/test_data_quality.py` 全量通过（包含 Parquet 用例）

2. 条件跳过方案：
- 对 Parquet 用例增加条件跳过（缺少引擎时 `skip` 而非 `fail`）
- 报告中明确“核心用例通过、扩展用例跳过”的分级

3. 回归输出分级（必做）：
- 输出通过 / 跳过 / 未运行 / 失败
- 至少执行：
  - `tests/test_workspace_consistency.py`
  - `tests/test_data_quality.py`

验收标准：
- 不再出现“同一口径下既称标准化完成又存在未解释失败”
- 测试策略被文档化并可复现
- 汇报证据与测试输出一致

---

## 五、范围限制（强约束）

- 只允许执行 TASK-007 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：20260328-135643-task-007-doc-and-test-baseline-alignment.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（文档片段/测试输出）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
