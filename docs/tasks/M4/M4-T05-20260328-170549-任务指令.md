# M4-T05 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/M4-T04-R-20260328-163352-runtime-baseline-repair-report.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：M4-T05
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（M4-T04）的实测审查，结论如下：

1. 已核验通过：
- `package.json` 已可被 pnpm 解析（不再是 JSON 语法错误）。
- `apps/api/tests/test_results_contract.py` 实测 `9 passed`。
- README 中“端到端验收前置条件/常见失败排查”文案已修正。

2. 关键问题（高优先级）：
- `pnpm test:e2e:results:json` 实际输出显示：
  - `split` 步骤成功；
  - 创建实验接口返回了完整实验对象（明显成功）；
  - 但脚本仍报错 `Failed to create experiment: {...}` 并退出失败。
- 同一次输出中“service_check 全部 unhealthy（404）”与后续业务步骤成功并存，说明服务健康检查逻辑存在错误或检查目标端点配置错误。

3. 结论：
- 当前 e2e 脚本“可执行”，但“成功判定逻辑不可信”，尚未达到可作为发布门禁的质量。

因此本轮目标：修复 e2e 脚本判定逻辑与健康检查逻辑，打通一次真实成功的端到端验收闭环。

---

## 三、Trae 多智能体协作（建议）

- backend-expert：修复 `e2e_validation.py` 的状态码/返回体判定与错误处理逻辑
- qa-engineer：复现并验证健康检查与步骤判定一致性，补充回归用例
- devops-architect：梳理前置服务探活端点与启动顺序，给出稳定执行条件
- tech-writer：同步 README 中 e2e 成功/失败判定标准与排查路径
- tech-lead-architect：统一验收口径与最终证据输出

说明：可自行增减智能体，但必须在汇报中写明分工、动作、证据与结论。

---

## 四、任务范围

### 任务 1：修复 e2e 脚本的成功判定与健康检查逻辑（必须完成）

目标：
- 让脚本输出与真实接口结果一致，不再出现“接口成功但脚本判失败”的假阴性。

必须完成：
1. 修复创建实验步骤判定：
- 正确处理创建实验接口的成功状态码与响应体（如 200/201 差异）
- 从返回体中稳定提取 `experiment_id`

2. 修复服务健康检查：
- 明确 API/Worker/Redis 的检查方式与端点
- 消除“健康检查全失败但业务步骤成功”的自相矛盾输出

3. 错误输出规范化：
- 失败时输出结构化原因（步骤名、HTTP 状态、响应摘要）
- 成功时输出完整步骤轨迹与关键产物路径

验收标准：
- 同一轮执行中，健康检查与业务步骤结果逻辑一致
- 不再出现“创建成功却报创建失败”
- 输出可直接用于验收报告引用

---

### 任务 2：完成一次真实的端到端成功闭环并固化回归（必须完成）

目标：
- 让 `pnpm test:e2e:results` 成为可用于发布前门禁的真实成功用例。

必须完成：
1. 前置服务准备并声明：
- 明确并验证 API / Worker / PostgreSQL / Redis 的运行状态

2. 端到端成功执行：
- 执行 `pnpm test:e2e:results`
- 执行 `pnpm test:e2e:results:json`
- 至少一次完整成功：split -> create_experiment -> start_training -> wait_for_completion -> get_results -> download_model

3. 回归补充：
- 新增至少 1 个针对 e2e 脚本判定逻辑的单元/集成测试，防止回归
- README 同步“成功判定标准”和“失败分支解释”

验收标准：
- 两个命令均可执行，其中至少一次全流程成功
- JSON 输出中 `success=true`，并含 experiment_id 与步骤明细
- 新增防回归测试通过

---

## 五、范围限制（强约束）

- 只允许执行 M4-T05 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：M4-T05-R-20260328-170549-e2e-logic-fix-and-success-closure-report.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（终端输出、JSON 输出、产物路径）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
