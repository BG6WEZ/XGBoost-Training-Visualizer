# M4-T04 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/task-008-20260328-results-contract-and-e2e-gate.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：M4-T04
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（TASK-008）的实测审查，结论如下：

1. 汇报与实测不一致（高优先级）：
- 汇报声称 `tests/test_results_contract.py` 为 9 passed。
- 实测结果为 9 errors，报错 `NameError: name 'Base' is not defined`。

2. 命令入口存在阻断（高优先级）：
- `package.json` 中 `test:e2e:results:json` 脚本字符串缺少闭合引号，导致 JSON 非法。
- 结果：`pnpm` 无法解析项目脚本（bad control character in string literal）。

3. 部分新增内容真实存在（中优先级）：
- `apps/api/tests/test_results_contract.py`、`apps/api/scripts/e2e_validation.py` 文件已存在。
- `README.md` 已有端到端验收章节，但标题文案出现“巻加”异常字符，需统一修正。

因此本轮目标：先恢复“可运行基线”（修复 package.json + 让契约测试真实通过），再补齐端到端脚本的可执行证据。

---

## 三、Trae 多智能体协作（建议）

- backend-expert：修复 `test_results_contract.py`（Base 导入/fixture 构造）并保证测试通过
- qa-engineer：执行回归验证与结果分级，产出可复现实测证据
- devops-architect：修复 `package.json` 脚本配置，恢复 pnpm 可解析
- tech-writer：修正 README 异常文案并同步验收说明
- tech-lead-architect：统一口径，防止“汇报通过但实测失败”再次发生

说明：可自行增减智能体，但必须在汇报中写明分工、动作、证据与结论。

---

## 四、任务范围

### 任务 1：恢复可运行基线（必须完成）

目标：
- 让项目重新具备“pnpm 可解析 + 契约测试可执行”的最小健康状态。

必须完成：
1. 修复 `package.json`：
- 修复 `test:e2e:results:json` 字符串闭合问题
- 验证 `pnpm` 可正常解析（至少执行一次 `pnpm -s run test:e2e:results:json --help` 或等效命令）

2. 修复契约测试：
- 修复 `apps/api/tests/test_results_contract.py` 中 `Base` 未定义问题
- 执行并通过：
  - `tests/test_results_contract.py`

3. 输出结果分级：
- 必须区分：通过 / 跳过 / 未运行 / 失败

验收标准：
- `pnpm` 可正常解析 `package.json`
- `tests/test_results_contract.py` 实测通过
- 汇报含命令和原始输出摘要

---

### 任务 2：补齐端到端验收脚本的真实性证据（必须完成）

目标：
- 证明 `pnpm test:e2e:results` 与 `pnpm test:e2e:results:json` 为真实可执行入口，不是仅文件存在。

必须完成：
1. 验证脚本执行：
- 执行 `pnpm test:e2e:results`（若服务未起，允许失败，但必须输出真实失败原因）
- 执行 `pnpm test:e2e:results:json`

2. README 修正：
- 修复“巻加端到端验收前置条件/排查”为规范中文标题
- 确保文档命令与 package.json 一致

3. 最小环境说明：
- 明确哪些前置服务必须运行（API/Worker/PostgreSQL/Redis）
- 明确无服务时脚本的预期报错行为

验收标准：
- 两个命令均可启动执行（成功或可解释失败）
- README 与命令入口一致
- 报告中有真实执行证据

---

## 五、范围限制（强约束）

- 只允许执行 M4-T04 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：M4-T04-R-20260328-163352-runtime-baseline-repair-report.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（测试输出、命令输出、文档片段）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
