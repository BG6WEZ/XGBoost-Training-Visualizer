# TASK-004 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/20260327-232731-task-003-workspace-unification-guard.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：TASK-004
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（TASK-003）的审查，本轮结论如下：

1. 代码级路径统一与一致性守卫已完成，且已有 9 个测试通过。
2. 关键闭环仍未完成：完整链路验证（切分 -> 训练 -> 结果读取）仍处于“未验证”。
3. 历史产物迁移仍是人工建议状态，尚未形成可复现、可审计的执行证据。
4. pytest 存在 `integration` 标记告警，说明测试治理仍有缺口。

因此本轮目标：完成“统一路径改造”的验收闭环，给出可复现证据并消除遗留告警。

---

## 三、Trae 多智能体协作（建议）

- system-architect：确认闭环验收范围与证据结构
- backend-expert：执行并固化产物迁移、配置核对、接口链路验证
- ai-ml-engineer：验证训练流程、模型产物落盘路径、结果读取完整性
- qa-engineer：补齐 integration 标记治理与自动化验证输出
- devops-architect：整理一键执行命令与环境前置检查
- tech-lead-architect：收口风险、审查证据、统一输出最终汇报

说明：可自行增减智能体，但必须在汇报中写明分工、执行动作与证据。

---

## 四、任务范围

### 任务 1：完成 workspace 统一后的全链路闭环验证（必须完成）

目标：
- 在“单一 workspace 根目录”前提下，完成一次可复现的端到端业务链路验证。

必须完成：
1. 历史产物迁移与校验：
- 执行迁移动作，将 legacy 路径下可用产物迁移到项目根 workspace 对应目录
- 迁移前后分别给出文件数量/关键文件清单对比
- 不允许覆盖导致关键历史结果丢失，若冲突必须记录冲突清单与处理策略

2. 全链路实测（至少 1 次）：
- 切分（splits）
- 训练（models）
- 结果读取/展示（至少覆盖 API 读取或前端结果接口）
- 每一步必须给出命令、返回摘要、产物路径证据

3. 路径一致性复核：
- 明确证明 API 与 Worker 实际运行写入的是同一根 workspace
- 给出关键产物绝对路径证据（示例：模型文件、切分文件）

验收标准：
- 完整链路至少 1 次通过
- 关键产物写入统一 workspace 根目录
- 汇报中可审计地复现全过程

---

### 任务 2：修复测试治理遗留并固化回归入口（必须完成）

目标：
- 消除当前告警并确保后续验证可持续复用。

必须完成：
1. 消除 pytest 标记告警：
- 在 pytest.ini 中正确注册 `integration`（或按团队规则重构标记）
- 重新执行相关测试并确认无同类告警

2. 固化回归入口：
- 提供至少一个可直接复用的验证入口（脚本或标准命令集）
- 覆盖：路径一致性测试 + 至少一条链路冒烟验证

3. 文档同步：
- 更新运行文档中“workspace 单一路径原则”和“回归命令”
- 明确“通过/跳过/未运行/失败”分级

验收标准：
- 不再出现当前已知 integration 标记告警
- 回归入口可一键或一步步执行且可复现
- 文档、实现、测试三者一致

---

## 五、范围限制（强约束）

- 只允许执行 TASK-004 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。
- 若发现阻塞（环境、数据、权限），必须在汇报中给出阻塞定位与最小解法，不得跳过不报。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：20260328-091828-task-004-workspace-e2e-closure.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（含路径/日志/截图或输出摘要）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
