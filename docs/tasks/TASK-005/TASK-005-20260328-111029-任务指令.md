# TASK-005 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/20260328-101820-task-004-workspace-e2e-closure.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：TASK-005
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（TASK-004）及仓库实测核验，结论如下：

1. 可复现且可信的部分：
- `apps/api/pytest.ini` 中 `integration` 标记已注册
- `apps/api/scripts/regression_check.py` 已存在且可执行
- 根目录 `README.md` 已补充回归命令与 workspace 说明
- `pnpm test:regression:path` 可执行并通过
- 当前目录计数与汇报一致：`apps/api/workspace=30`、`apps/worker/workspace=4`、`workspace=47`

2. 尚未闭环且存在口径风险的部分：
- 报告写有“任务1已验证通过”，但同文又写“训练未成功、模型产物写入路径未验证”，属于验收口径不一致
- “全链路闭环”尚未达到“训练成功 + 模型产物落盘可证据化”标准

3. 新发现问题：
- pytest 启动时出现 `WARNING: ignoring pytest config in pyproject.toml!`，测试配置存在双源/冲突风险，需治理

因此本轮目标：完成真正意义上的训练成功闭环，并修复测试配置冲突与验收口径问题。

---

## 三、Trae 多智能体协作（建议）

- system-architect：定义“闭环完成”的可验收标准与证据模板
- backend-expert：修复训练链路阻塞（数据校验/容错）并输出 API 证据
- ai-ml-engineer：保障至少 1 次训练成功，验证模型产物落盘与可读取
- data-engineer：处理目标列 NaN（清洗、过滤或替代数据集），并保留前后差异证据
- qa-engineer：治理 pytest 配置冲突，固化稳定回归入口
- tech-lead-architect：统一验收口径，收口风险并输出最终报告

说明：可自行增减智能体，但必须在汇报中写明分工、动作、证据与结论。

---

## 四、任务范围

### 任务 1：完成“训练成功 + 模型产物落盘”的真闭环（必须完成）

目标：
- 在统一 workspace 根目录下，完成至少 1 次成功训练并可证据化读取结果。

必须完成：
1. 数据质量阻塞处置：
- 明确目标列 NaN 的处置策略（过滤/填充/替换数据集）
- 给出处置前后统计（样本数、NaN 数）

2. 成功训练闭环：
- 至少 1 次完整链路成功（切分 -> 训练成功 -> 结果读取）
- 给出关键命令、API 返回摘要、日志片段

3. 产物路径证据：
- 明确给出模型产物文件名与绝对路径
- 证明模型产物写入项目根 `workspace/models`（非 legacy 目录）

验收标准：
- 至少 1 次训练状态为成功
- 模型产物可在根 workspace 中定位并可读取
- 证据可复现

---

### 任务 2：治理 pytest 配置冲突并统一验收口径（必须完成）

目标：
- 消除测试配置歧义，避免“结果通过但配置被忽略”的隐患，并修正报告口径。

必须完成：
1. 配置治理：
- 排查并处理 `pytest.ini` 与 `pyproject.toml` 的 pytest 配置冲突
- 确保执行 pytest 时不再出现“ignoring pytest config in pyproject.toml”警告

2. 回归验证：
- 至少执行：路径一致性测试 + 关键链路冒烟（可沿用回归脚本）
- 输出通过/失败/跳过/未运行分级

3. 口径修正：
- 汇报中不得出现“任务已验证通过”与“关键验收项未验证”并存的表述
- 明确“完成定义（DoD）”并据此给出最终结论

验收标准：
- pytest 配置警告消除
- 回归验证可复现
- 汇报口径与证据一致

---

## 五、范围限制（强约束）

- 只允许执行 TASK-005 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：20260328-111029-task-005-training-success-closure.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（路径/日志/API 返回摘要）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
