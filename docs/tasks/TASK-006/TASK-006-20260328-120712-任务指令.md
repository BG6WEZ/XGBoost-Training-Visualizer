# TASK-006 任务指令（2026-03-28）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/20260328-111029-task-005-training-success-closure.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：TASK-006
- 下发时间：2026-03-28
- 生效时间：立即
- 执行窗口：2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报（TASK-005）的实测审查，本轮结论如下：

1. 报告高可信项（已复现）：
- `apps/api/pyproject.toml` 已移除 pytest 配置块，`pytest.ini` 成为唯一配置源
- 报告声明模型文件真实存在：`workspace/models/7740bebd-8b2a-4647-a92f-f32ba3afb700.json`，大小 93042 bytes
- `tests/test_workspace_consistency.py + tests/test_data_quality.py` 共 28 项测试可复现通过
- 运行 pytest 时已无 `ignoring pytest config in pyproject.toml` 警告

2. 仍待闭环问题：
- 报告中已标注 `GET /api/experiments/{id}/model` 返回 404，说明“训练成功后模型读取 API”仍有功能缺口

3. 新发现工程风险：
- 项目 `.venv` 中缺少 pytest（`No module named pytest`），当前验证依赖系统 Python，环境可复现性不足

因此本轮目标：修复模型读取 API 缺口，并完成测试环境标准化，确保“可训练 + 可读取 + 可复现验证”三者闭环。

---

## 三、Trae 多智能体协作（建议）

- system-architect：定义模型读取链路的完成标准与证据模板
- backend-expert：修复模型记录关联与模型查询接口逻辑
- ai-ml-engineer：验证训练产物与模型元数据一致性
- qa-engineer：新增/补强模型读取相关测试与回归用例
- devops-architect：修复并固化 Python 环境依赖（.venv 可独立跑测试）
- tech-lead-architect：统一验收口径并输出最终报告

说明：可自行增减智能体，但必须在汇报中写明分工、动作、证据与结论。

---

## 四、任务范围

### 任务 1：修复模型读取 API 404 并形成端到端读取闭环（必须完成）

目标：
- 确保训练完成后可通过 API 稳定读取对应模型信息/产物。

必须完成：
1. 根因定位：
- 排查模型保存与实验关联逻辑（如 `experiment_id`、模型记录写入时机、查询条件）
- 明确 404 的直接原因并记录定位证据

2. 代码修复：
- 修复 `GET /api/experiments/{id}/model` 404 问题
- 保证训练完成后可返回模型信息（至少包含模型 ID/路径/存储类型）

3. 闭环验证：
- 执行“训练成功 -> API 读取模型”至少 1 次
- 给出请求命令、返回摘要、模型文件路径与记录一致性证据

验收标准：
- `GET /api/experiments/{id}/model` 不再误报 404
- 返回数据与 `workspace/models` 实际文件一致
- 证据可复现

---

### 任务 2：标准化测试环境，消除系统 Python 依赖（必须完成）

目标：
- 让项目 `.venv` 能独立执行关键测试，避免“换机不可复现”。

必须完成：
1. 依赖补齐：
- 确保 `.venv` 内具备 pytest 与当前测试所需最小依赖
- 不依赖系统 Python 的全局包完成测试

2. 回归验证（必须在 .venv 执行）：
- `tests/test_workspace_consistency.py`
- `tests/test_data_quality.py`
- 输出通过/失败/跳过/未运行分级

3. 文档同步：
- 在 README 或开发文档中明确 `.venv` 启用与测试命令
- 明确“优先使用项目虚拟环境”原则

验收标准：
- `.venv` 中可直接运行关键测试并通过
- 不再依赖系统 Python 的 pytest 安装
- 文档与实际执行方式一致

---

## 五、范围限制（强约束）

- 只允许执行 TASK-006 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：20260328-120712-task-006-model-api-and-venv-closure.md

2. 汇报必须包含：
- 已完成任务编号
- 内部智能体分工与执行记录
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据（请求返回/日志/文件路径）
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
