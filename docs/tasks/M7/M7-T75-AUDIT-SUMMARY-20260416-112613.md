# M7-T75 审计总结 — Phase-3 / Task 3.2 再收口（真实业务 E2E + 诚实报告）核验

时间戳：20260416-112613  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T75-R-20260416-111500-p3-t32-real-business-e2e-closure-report.md` 与当前 `apps/web/e2e/` 实际状态  
审计结论：❌ **不通过（继续停留在 Task 3.2 收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T75-20260416-095303-p3-t32-real-business-e2e-closure.md`
- `docs/tasks/M7/M7-T75-R-20260416-111500-p3-t32-real-business-e2e-closure-report.md`
- `apps/web/package.json`
- `apps/web/playwright.config.ts`
- `apps/web/e2e/auth.spec.ts`
- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`
- `apps/web/e2e/screens/` 实际截图目录

---

## 二、通过项

### 1. 报告文件编号已与本轮任务编号对齐

当前已存在：

- `docs/tasks/M7/M7-T75-R-20260416-111500-p3-t32-real-business-e2e-closure-report.md`

说明本轮至少补上了与任务编号一致的正式报告文件。

### 2. 资产流程较上一轮有实质补强

`assets.spec.ts` 已较上一轮前进到：

- 通过 API 登录获取 token
- 通过 multipart 上传 CSV
- 对上传接口返回字段做断言
- 通过 API 创建数据集并校验列表

说明资产相关弱验证已开始向真实动作 + 真实断言收敛。

---

## 三、不通过项

### 1. 报告声称“零静默跳过”，但代码中仍实际存在静默跳过逻辑

`M7-T75-R` 报告明确宣称：

- “zero silent skip patterns”
- “The following patterns have been completely removed”

但当前实际代码中仍可直接检出：

- [experiments.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/experiments.spec.ts#L62-L75)
- [results.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/results.spec.ts#L68-L85)

其中仍包含：

- `if (await tableElement.isVisible({ timeout: 3000 }).catch(() => false)) { ... }`
- `const isVisible = await experimentLink.isVisible({ timeout: 3000 }).catch(() => false); if (isVisible) { ... }`

结论：

- “不存在静默跳过式通过逻辑”这一通过条件未满足
- 报告对当前代码状态的描述不真实

### 2. 实验与结果流程仍未达到任务单要求的真实业务主路径

`M7-T75` 明确要求：

- 实验流程要做到“真实创建 + 列表出现 + 提交训练 + 真实断言”
- 结果流程要绑定真实实验数据，并验证指标/对比状态变化

但当前实际代码仍停留在弱验证：

- [experiments.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/experiments.spec.ts#L33-L85) 主要还是“按钮可见”“表单出现”“页面可访问”
- [results.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/results.spec.ts#L22-L85) 仍有“直接 `goto()` 固定页面 + 可见性断言”的模式
- 报告自身也在“未验证部分”承认未做真实实验创建、未做真实训练提交、未做真实对比页验证，可见 [M7-T75-R](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/docs/tasks/M7/M7-T75-R-20260416-111500-p3-t32-real-business-e2e-closure-report.md#L122-L138)

结论：

- “资产 / 实验 / 结果主路径为真实动作 + 真实断言”这一核心通过条件未满足

### 3. 报告与实际截图目录不一致

`M7-T75-R` 报告写明：

- “All 20 screenshots collected”
- 列出了 `TC8-upload-success.png`

但当前 `apps/web/e2e/screens/` 实际核验仅见 19 张截图，且存在：

- `TC8-upload-attempt.png`

并未看到报告中声称的：

- `TC8-upload-success.png`

结论：

- “截图证据实际生成并与报告描述一致”未满足
- 报告再次出现证据与仓库现状不一致的问题

### 4. 报告把“未验证”事项包装成“全部审计问题已解决”

报告结论写明：

- “All M7-T74 audit issues have been resolved.”

但同一份报告又明确承认：

- 未做真实实验创建 + 训练
- 未验证质量报告内容正确性
- 未验证真实实验对比页

这与 `M7-T75` 任务单要求的“真实业务主路径”并不一致。

结论：

- 当前报告仍存在把未完成验证包装成“问题已全部解决”的表述风险

---

## 四、审计结论

- **状态：不通过（Fail）**
- **主要原因：**
  1. 代码中仍保留静默跳过式通过逻辑
  2. 实验 / 结果流程仍未落成真实业务主路径
  3. 报告与截图证据不一致
  4. 报告对完成度的表述仍超出当前代码与验证事实

因此：

- **M7-T75 不予验收通过**
- **不得进入 Task 3.3**
- **下一轮仍必须停留在 Task 3.2 收口范围内**

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T76-20260416-112613-p3-t32-experiments-results-real-flow-closure.md`

下一轮必须聚焦：

1. 删除 `experiments.spec.ts` 与 `results.spec.ts` 中残留的静默跳过逻辑
2. 用真实数据准备把实验创建、训练提交、结果查看、对比页变成可失败的真实主路径
3. 确保截图目录与报告文件名、截图文件名完全一致
4. 在报告中严格区分“已验证通过”与“仍未验证”，不得再写“全部问题已解决”式结论
