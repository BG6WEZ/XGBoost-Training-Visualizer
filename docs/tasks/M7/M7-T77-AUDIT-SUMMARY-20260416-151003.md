# M7-T77 审计总结 — Phase-3 / Task 3.2 再收口（结果断言与证据对齐）核验

时间戳：20260416-151003  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T77-R-20260416-145300-p3-t32-results-assertion-and-evidence-alignment-report.md`  
审计结论：✅ **通过（Task 3.2 验收通过，可进入 Task 3.3）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T77-20260416-143947-p3-t32-results-assertion-and-evidence-alignment.md`
- `docs/tasks/M7/M7-T77-R-20260416-145300-p3-t32-results-assertion-and-evidence-alignment-report.md`
- `apps/web/package.json`
- `apps/web/playwright.config.ts`
- `apps/web/e2e/auth.spec.ts`
- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`
- `apps/web/e2e/screens/` 实际截图目录

---

## 二、通过项

### 1. 截图证据与报告已对齐

当前 `apps/web/e2e/screens/` 实际落盘 19 张截图，且与 `M7-T77-R` 报告列出的文件名一致：

- 包含 `TC8-upload-success.png`
- 不再出现上一轮“报告列出但目录不存在”的情况
- `TC4` 未截图这一事实已在报告中如实说明

满足本轮“证据与报告逐项一致”的要求。

### 2. 结果流程已新增真实结果断言

`results.spec.ts` 中 `TC20` 已不再停留于“页面可见/包含实验名”，而是：

- 先通过 API 获取真实实验数据
- 再使用真实 `experimentId` 打开详情页
- 断言页面包含 API 返回的 `status`
- 断言页面包含配置值 `max_depth = 3`

这满足了本轮“至少一个真实结果断言”的要求。

### 3. 对比页已新增真实状态变化断言

`TC17` 当前已实现：

- 读取 checkbox 初始状态
- 点击 checkbox
- 断言勾选状态发生变化
- 断言至少一个 checkbox 处于选中状态

在当前通过结果下，这已经构成“动作前 vs 动作后”的差异性断言，满足本轮最小要求。

### 4. 实验 / 结果文件中未再检出上一轮阻断的静默跳过模式

当前未再检出 `experiments.spec.ts` / `results.spec.ts` 中上一轮明确阻断的：

- `catch(() => false)` 关键路径跳过逻辑
- `if exists then check, otherwise silently pass` 的实验/结果主路径写法

满足本轮任务单对这两个文件的要求。

### 5. 报告已按事实区分已验证与限制项

`M7-T77-R` 当前已明确区分：

- 已验证通过项
- 未验证 / 限制项

例如质量报告内容校验、真实训练提交、真实对比指标等缺口均已如实写明，不再把这些内容包装成“当前已验证完成”。

### 6. `test:e2e` 与用例规模保持达标

当前仍满足：

- `apps/web/package.json` 中存在 `test:e2e`
- E2E 用例数为 20
- 覆盖登录 / 数据资产 / 实验 / 结果查看四类流程

与 Task 3.2 的目标范围一致。

---

## 三、审计结论

- **状态：通过（Pass）**
- **原因：**
  1. 截图证据与报告已一致
  2. 结果流程已补上真实结果断言
  3. 对比页已补上真实状态变化断言
  4. 实验 / 结果文件中上一轮阻断的静默跳过问题已消除
  5. 报告已较诚实地区分已验证与未验证项

因此：

- **Task 3.2 验收通过**
- **允许进入 Task 3.3（API 集成冒烟测试脚本）**

---

## 四、说明

仍存在一些非阻断限制：

- 质量报告内容正确性仍受后端既有 bug 影响
- 真实训练提交与真实对比指标验证仍未纳入本轮通过结论
- `auth.spec.ts` 中仍有条件分支式登出处理，但不属于本轮 `T77` 的核心阻断范围

这些风险已在报告中如实披露，不构成继续停留在 Task 3.2 的阻断项。

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T78-20260416-151003-p3-t33-api-smoke-test-script.md`

下一轮进入：

1. API 集成冒烟测试脚本
2. 覆盖认证、数据集、实验、训练、结果、对比、登出链路
3. 输出 PASS/FAIL + 耗时，并以退出码反映结果
