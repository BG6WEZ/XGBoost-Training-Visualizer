# M7-T73 审计总结 — Phase-3 / Task 3.2 前端 E2E 测试（Playwright）核验

时间戳：20260416-085339  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T73-R-20260416-084000-p3-t32-frontend-e2e-playwright-report.md`  
审计结论：❌ **不通过（需继续停留在 Task 3.2 范围内收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T73-20260415-163004-p3-t32-frontend-e2e-playwright.md`
- `docs/tasks/M7/M7-T73-R-20260416-084000-p3-t32-frontend-e2e-playwright-report.md`
- `apps/web/package.json`
- `apps/web/playwright.config.ts`
- `apps/web/e2e/auth.spec.ts`
- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`
- `apps/web/e2e/` 目录结构与截图目录核验

---

## 二、通过项

### 1. Playwright 基础接线已落地

当前已完成：

- `apps/web/package.json` 中加入 `@playwright/test`
- 新增 `test:e2e` 脚本
- `apps/web/playwright.config.ts` 已存在并配置了 `baseURL`、`webServer`、截图/trace/video 策略

### 2. 测试文件数量和名义用例数达标

当前已有 4 个 E2E 文件：

- `auth.spec.ts`
- `assets.spec.ts`
- `experiments.spec.ts`
- `results.spec.ts`

按文件内 `test(...)` 数量统计，总用例数为 22 个，满足 “不少于 10 个” 的数量要求。

### 3. 四类页面路径已被触达

从代码结构上看，当前已经尝试覆盖：

- 登录/登出
- 数据资产
- 实验
- 结果查看

说明本轮并非完全偏题，而是已经搭起了 E2E 框架和页面入口。

---

## 三、不通过项

### 1. 核心阻断：任务单要求的 `npx playwright test` 全部通过并未实际完成

M7-T73 任务单通过条件明确要求：

- `npx playwright test` 全部 passed

但报告中已明确写出：

- “由于 Docker 未运行... E2E 测试无法实际执行。”

结论：

- 本轮最关键的验收条件未满足。
- 不能以“代码已编写完成”替代“测试已执行通过”。

### 2. 核心阻断：截图证据未实际生成

任务单明确要求：

- 截图证据自动生成

我实际核验 `apps/web/e2e/` 目录时，仅看到：

- `assets.spec.ts`
- `auth.spec.ts`
- `experiments.spec.ts`
- `results.spec.ts`

而：

- `apps/web/e2e/screenshots/` 下没有任何实际文件
- 甚至当前目录核验中未看到该目录落地

结论：

- “截图证据自动生成”当前并未成立。

### 3. 多个“流程测试”并未验证任务单要求的真实业务行为

任务单要求的是：

- 登录成功/失败/登出
- 数据资产扫描、上传 CSV、注册数据集
- 创建实验、实验列表显示、提交训练任务
- 结果页加载、指标可见、实验对比

但当前多个测试只做到“打开页面/截图/看标题”，并未执行关键动作。例如：

- [assets.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/assets.spec.ts#L45-L55) 只检查上传按钮可见，没有上传文件
- [assets.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/assets.spec.ts#L72-L78) 直接 `goto('/assets/1/quality')` 并截图，没有验证数据集注册成功
- [experiments.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/experiments.spec.ts#L43-L57) 只尝试打开表单，没有填写参数并提交训练
- [results.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/results.spec.ts#L21-L35) 只打开实验详情并截图，没有断言指标数据可见

结论：

- 当前不少用例是“页面导航/元素可见性测试”，不是任务单要求的“关键业务流程 E2E”。

### 4. 存在静默跳过式测试，可能在核心步骤未发生时仍然通过

多个测试使用了如下模式：

- `if (await xxx.isVisible(...).catch(() => false)) { ... }`

例如：

- [auth.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/auth.spec.ts#L75-L94)
- [assets.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/assets.spec.ts#L62-L69)
- [experiments.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/experiments.spec.ts#L47-L53)
- [results.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/results.spec.ts#L27-L31)

如果目标元素不存在：

- 测试会直接跳过关键动作
- 且没有 `fail()` / 强断言

结论：

- 这类测试可能在“关键流程根本没有执行”的情况下仍然显示通过，属于低可信度用例。

### 5. 截图路径与报告描述不一致

报告写的是：

- 截图输出路径为 `apps/web/e2e/screenshots/`

但实际代码中的手动截图路径是：

- `e2e/screens/*.png`

而 Playwright 配置中的自动截图策略又是：

- `screenshot: 'only-on-failure'`

结论：

- 当前“截图目录/截图来源/截图规则”三者并不一致。
- 报告对截图证据位置的描述不准确。

---

## 四、审计结论

- **状态：不通过（Fail）**
- **原因：**
  1. `npx playwright test` 未实际执行通过
  2. 截图证据未实际生成
  3. 多个测试未覆盖任务单要求的真实业务行为
  4. 存在静默跳过式测试，可信度不足
  5. 报告中的截图路径与代码实现不一致

因此：

- **M7-T73 不予验收通过**
- **不得进入 Task 3.3**
- 下一轮仍必须停留在 **Task 3.2 收口** 范围内

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T74-20260416-085339-p3-t32-playwright-execution-and-flow-hardening.md`

下一轮必须聚焦：

1. 在完整环境中真实执行 `npx playwright test`
2. 生成并提交实际截图证据
3. 把“只看页面/截图”的弱用例补强为真实业务流程验证
4. 去掉静默跳过式通过逻辑
