# M7-T56 范围文档：Task 1.1 统一密钥管理，移除所有硬编码 Secret

任务编号: M7-T56 (对应 LAUNCH_DEVELOPMENT_PLAN Task 1.1)  
时间戳: 20260413-151500  
所属计划: LAUNCH_DEVELOPMENT_PLAN / Phase-1 Task 1.1

---

## 任务范围

### 目标
统一密钥管理，移除所有硬编码 Secret，确保 JWT_SECRET 和 DATABASE_URL 必须通过环境变量或 .env 文件显式配置。

### 范围文件
- `apps/api/app/config.py`
- `apps/api/app/services/auth.py`
- `apps/api/tests/conftest.py`
- `apps/api/tests/test_auth.py`
- `.env.example`
- `.gitignore`（仅确认，无需修改）

### 非范围文件
- 其他业务逻辑代码
- 前端代码
- Worker 代码

---

## 修改文件清单

| 文件路径 | 修改目的 | 变更类型 |
|---------|---------|---------|
| `apps/api/app/config.py` | JWT_SECRET/DATABASE_URL 默认值改空，增加 model_post_init 校验 | 修改 |
| `apps/api/app/services/auth.py` | 删除硬编码 SECRET_KEY，移除条件分支，统一从 config 读取 | 修改 |
| `apps/api/tests/conftest.py` | 添加 JWT_SECRET 测试环境变量 | 修改 |
| `apps/api/tests/test_auth.py` | 新增 TestJwtSecretValidation 类（2 个测试） | 修改 |
| `.env.example` | 重写为完整版，包含 15+ 环境变量且有分类注释 | 重写 |

---

## 通过条件

- [x] `SECRET_KEY` 在 `auth.py` 中不再存在硬编码值
- [x] `config.py` 中 `JWT_SECRET` 默认值为空且有启动校验
- [x] `.env.example` 文件包含 12+ 个环境变量定义（实际 15+ 个）
- [x] 现有 338 个测试全部通过（基线 336 + 新增 2）

---

## 关联文档

- 验收报告: `M7-T56-1.1-fix-VERIFICATION.md`
- 环境变量模板: `.env.example`