# M7-T79 — Phase-3 / Task 3.3 再收口（Worker 感知的 API 冒烟闭环）

> 任务编号：M7-T79  
> 阶段：Phase-3 / Task 3.3 Re-open  
> 前置：M7-T78（审计不通过）  
> 时间戳：20260416-153139

---

## 零、开始前必须先做

- [ ] 阅读 `docs/collaboraion/CLAUDE_WORK_RULES.md`
- [ ] 阅读 `docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md`
- [ ] 阅读 `docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md`
- [ ] 阅读 `docs/tasks/M7/M7-T78-AUDIT-SUMMARY-20260416-153139.md`
- [ ] 阅读 `docs/planning/LAUNCH_DEVELOPMENT_PLAN.md` 中 `Task 3.3`

---

## 一、本轮目标

本轮**不得进入 Phase-4**。只允许继续收口 **Task 3.3 API 集成冒烟测试脚本**，重点解决：

1. 真实执行仍有失败步骤  
2. 退出码不是 `0`  
3. 总耗时超过 `120s`

---

## 二、允许修改的范围文件

- `scripts/smoke-test-api.py`
- 如确有必要，可补充与脚本运行直接相关的最小说明
- 本轮新增报告文件：`docs/tasks/M7/M7-T79-R-<timestamp>-p3-t33-worker-aware-api-smoke-closure-report.md`

禁止越界到：

- API / Worker 业务逻辑实现
- 前端 Playwright 用例
- Phase-4 或部署相关内容

---

## 三、上一轮未通过的核心问题

你必须逐项消除以下问题：

1. **步骤 9 真实失败**
   - 训练轮询超时
   - 退出码因此为 `1`

2. **总耗时超限**
   - 当前真实结果为 `129.27s`
   - 任务单要求 `< 120s`

3. **报告结论超前**
   - 不能再在 exit code `1`、总耗时超限时写“建议提交验收”

---

## 四、必须完成的最小工作

### 1) 在真实可执行环境下完成完整链路

本轮必须优先解决训练步骤无法完成的问题。

可接受方式：

- 启动 Worker 并完成真实训练消费
- 或在不修改业务逻辑的前提下，使用现有可运行环境让训练任务真实完成

不接受：

- 仅因为“脚本没问题”就宣称任务完成
- 跳过步骤 9 仍声称全链路完成

### 2) 真实执行结果必须达到 exit code `0`

要求：

- 脚本最终退出码为 `0`
- 不得存在任何失败步骤

### 3) 总耗时必须压到 `< 120s`

本轮必须实际满足：

- 总耗时 `< 120 秒（含训练等待）`

可接受优化方向：

- 缩短训练轮询等待
- 缩小测试数据规模
- 优化训练配置为更轻量的 smoke 参数

前提：

- 不能牺牲链路真实性
- 不能通过跳过关键链路来换时间

### 4) 报告必须严格以最终结果为准

报告中必须明确写清：

- 实际命令
- 实际退出码
- 实际总耗时
- 每一步 PASS/FAIL

禁止：

- exit code 不是 `0` 时仍建议提交验收
- 总耗时超限时仍写“已达标”

### 5) 必须真实执行脚本

至少执行：

```bash
python scripts/smoke-test-api.py --api-url http://localhost:8000
```

要求：

- 报告中附完整原始输出
- 若做过多次执行，必须明确哪一次作为最终验收结果

---

## 五、通过条件（全部满足才算通过）

- [ ] `scripts/smoke-test-api.py` 仍可独立运行
- [ ] 覆盖 ≥ 10 个 API 端点
- [ ] 每步输出 PASS/FAIL + 耗时
- [ ] 真实执行无失败步骤
- [ ] 实际退出码为 `0`
- [ ] 总耗时 < 120 秒（含训练等待）
- [ ] 本地全栈启动后脚本真实执行
- [ ] 产出与本轮编号一致的 `M7-T79-R-...` 报告
- [ ] 未越界推进到 Phase-4 或后续任务

---

## 六、汇报要求

完成后提交：

- `M7-T79-R-<timestamp>-p3-t33-worker-aware-api-smoke-closure-report.md`

汇报必须包含：

1. 已完成任务编号
2. 修改文件清单
3. 为何本轮能够让训练步骤完成
4. 脚本实际执行命令
5. 完整原始输出
6. 实际退出码
7. 实际总耗时
8. 已验证通过项
9. 未验证部分
10. 风险与限制
11. 是否建议提交 Task 3.3 验收

---

## 七、明确禁止

- 不得跳过步骤 9 却声称全链路通过
- 不得在 exit code `1` 时写“可验收通过”
- 不得在总耗时超限时写“满足任务门槛”
- 不得提前进入 Phase-4
