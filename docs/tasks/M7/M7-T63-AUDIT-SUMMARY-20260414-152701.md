# M7-T63 审计总结 — Task 2.1 补测核验

时间戳：20260414-152701  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T63-R-20260414-112300-p2-t21-audit-fix-large-upload-timeout-report.md`  
审计结论：❌ **不通过（需留在 Task 2.1 范围内继续修复）**

---

## 一、核验范围

本轮按以下材料交叉核验：

- `docs/tasks/M7/M7-T63-P2-T21-Audit-Fix-Large-Upload-Timeout-20260413-173000.md`
- `docs/tasks/M7/M7-T63-R-20260414-112300-p2-t21-audit-fix-large-upload-timeout-report.md`
- `apps/api/tests/test_datasets.py`
- `apps/api/app/routers/datasets.py`
- 实际复跑：
  - `..\..\.venv\Scripts\python.exe -m pytest tests/test_datasets.py::TestLargeFileUpload::test_upload_large_csv_does_not_block -q --tb=short -s`

说明：上面的命令在实际执行时使用的是正确路径 `..\..\.venv\Scripts\python.exe`。

---

## 二、通过项

1. 已补充“上传大文件”相关测试
- `apps/api/tests/test_datasets.py` 中已存在 `TestLargeFileUpload::test_upload_large_csv_does_not_block`。

2. 测试可运行
- 实际复跑结果为：
  - `Large file upload: 10.5MB, elapsed=34.05s, row_count=500000`
  - `1 passed in 39.40s`

3. 当前实现仍保留三段策略
- `apps/api/app/routers/datasets.py` 仍按 `<10MB / 10-100MB / >100MB` 进行行数处理分支。

---

## 三、不通过项

### 1. 关键验收口径未满足：测试文件不达任务要求的 >50MB

任务单要求是：

- “上传 >50MB 文件的超时测试”，或至少提供 >50MB / >100MB 的真实链路证据。

实际测试代码中：

- 目标大小写为 `15 * 1024 * 1024`
- 又被 `min(num_lines - 1, 500000)` 截断
- 实际复跑输出文件大小仅 **10.5MB**

结论：

- 当前测试不能作为 `>50MB` 非阻塞证据。

### 2. “30s 内返回”未被测试断言，且实测输出已超过 30s

任务单与报告都把“30s 阈值”作为验收口径，但测试中：

- 只记录 `elapsed`
- **没有** `assert elapsed < 30`

同时，实际复跑输出为：

- `elapsed=34.05s`

结论：

- 当前测试无法证明“30s 内返回”。
- 报告中“实际 API 处理时间约 30s 内”的说法没有来自测试断言或独立日志的直接证据，不能作为通过依据。

### 3. 父任务通过条件仍未满足：`upload_file()` 中仍有同步 `open()` 调用

`LAUNCH_DEVELOPMENT_PLAN` Task 2.1 明确要求：

- `upload_file()` 中无同步 `open()` 调用

当前代码中仍存在：

- `apps/api/app/routers/datasets.py`
  - `row_count = sum(1 for _ in open(file_path, encoding='utf-8')) - 1`

结论：

- 即使补测成立，Task 2.1 的父级通过条件仍未闭合，不能推进到 Task 2.2。

### 4. 报告把未验证项写成已满足

报告中勾选了：

- “触发估算分支时 `estimated=true`”

但同一份报告又明确说明：

- 本次测试使用的是 10.5MB 文件
- `>100MB` 分支未在自动化测试中覆盖

结论：

- 该项最多只能写“代码已实现、当前轮未验证”，不能勾选为已满足。

---

## 四、审计结论

- **状态：不通过（Fail）**
- **原因：**
  1. 核心证据口径不达标，未覆盖 >50MB
  2. 未断言 30s 阈值，且实测输出已超过 30s
  3. `upload_file()` 仍残留同步 `open()`，父任务通过条件未满足
  4. 报告对 `estimated=true` 的表述超出实际验证范围

因此：

- **M7-T63 不予验收通过**
- **不得进入 Task 2.2（CORS 配置生产化）**
- 下一轮必须继续停留在 **Task 2.1 收口** 范围内

---

## 五、下一步要求

已生成下一轮任务单：

- `docs/tasks/M7/M7-T64-20260414-152701-p2-t21-reopen-large-upload-proof-and-sync-open-cleanup.md`

下一轮目标不是新功能扩展，而是：

1. 补齐真实达标的 >50MB 非阻塞证据
2. 移除 `upload_file()` 中剩余同步 `open()` 调用
3. 按真实验证结果重写报告，不得把“代码推断”写成“已验证”
