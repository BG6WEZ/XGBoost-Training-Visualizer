# M7-T94 审计总结 — Phase-5 / Task 5.2 性能基线测试核验

时间戳：20260417-092530  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T94-R-20260417-092200-p5-t52-performance-baseline-report.md`、`scripts/benchmark.py`、`docs/planning/LAUNCH_DEVELOPMENT_PLAN.md` 中 `Task 5.2`  
审计结论：❌ **不通过（Task 5.2 未收口，禁止进入 Task 5.3）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T94-20260417-091530-p5-t52-performance-baseline.md`
- `docs/tasks/M7/M7-T94-R-20260417-092200-p5-t52-performance-baseline-report.md`
- `scripts/benchmark.py`
- `docs/planning/LAUNCH_DEVELOPMENT_PLAN.md` 中 `Task 5.2`

---

## 二、已成立的部分

### 1. 性能基线脚本已新增并落盘

当前仓库已存在：

- `scripts/benchmark.py`

脚本已覆盖本轮要求的 5 个端点：

- `GET /health`
- `POST /api/auth/login`
- `GET /api/datasets/`
- `GET /api/experiments/`
- `POST /api/datasets/upload`

并且输出了：

- 总请求数
- 成功数 / 失败数
- 错误率
- 平均延迟
- P95 延迟
- 最大延迟
- 5xx 统计

说明本轮“基线测量工具”已经建立。

### 2. 报告已明确给出 5xx 与成功率

`M7-T94-R` 已明确写出：

- `5xx 错误总计 = 0`
- 连接超时 `0`
- 读超时 `0`
- 认证失败 `0`
- 整体请求成功率 `100%`

这说明当前服务稳定性在本轮压测范围内没有出现明显错误。

### 3. 报告文件与本轮编号一致

当前仓库已存在：

- `docs/tasks/M7/M7-T94-R-20260417-092200-p5-t52-performance-baseline-report.md`

形式交付物完整存在。

---

## 三、阻断项

### 1. `Task 5.2` 的核心通过条件未满足

总计划 `Task 5.2` 的通过条件写得非常明确：

- `所有端点满足 P95 目标`
- `无 5xx 错误`
- `输出性能基线报告`

虽然你已经满足了：

- 无 `5xx`
- 已输出基线报告

但最关键的一项 **“所有端点满足 P95 目标”** 并未满足。

### 2. 报告已明确承认 5 / 5 端点全部未达标

`M7-T94-R` 的汇总表明确记录：

- `/health` → `2251.78ms`，目标 `< 50ms`，`FAIL`
- `/api/auth/login` → `21779.15ms`，目标 `< 500ms`，`FAIL`
- `/api/datasets/` → `942.16ms`，目标 `< 200ms`，`FAIL`
- `/api/experiments/` → `758.84ms`，目标 `< 200ms`，`FAIL`
- `/api/datasets/upload (1MB CSV)` → `5411.11ms`，目标 `< 3000ms`，`FAIL`

也就是说，本轮不是“少数边缘未达标”，而是 **5 / 5 全部未达标**。

### 3. `benchmark.py` 的退出码语义也表明当前结果不能视为通过

当前脚本末尾逻辑为：

```python
all_pass = all(r.meets_target for r in results)
if all_pass:
    sys.exit(0)
else:
    sys.exit(1)
```

由于报告已明确 `5 / 5` 端点均未达标，因此本轮最终执行按脚本设计应为：

- `exit code 1`

这进一步说明当前结果只能作为“失败基线”或“待优化前基线”，不能作为 `Task 5.2 已通过` 的验收依据。

### 4. 报告中的“建议提交 Task 5.2 验收”与计划和事实冲突

`M7-T94-R` 第十节写的是：

- `建议：可以提交 Task 5.2 验收`

但同一份报告又明确写出：

- `5/5 端点 P95 延迟均高于目标值`

因此该建议与：

- 总计划的通过条件
- 报告自身的数据
- 脚本退出码语义

三者均不一致，不能采信。

---

## 四、审计结论

- **状态：不通过（Fail）**
- **原因：**
  1. `Task 5.2` 要求所有端点满足 P95 目标
  2. 本轮 `5 / 5` 端点全部未达标
  3. `benchmark.py` 的设计语义下，本轮结果应为 `exit code 1`
  4. 报告“可提交验收”的结论与计划和数据相冲突

因此：

- **Task 5.2 未通过**
- **不得进入 Task 5.3**
- **必须继续停留在 Task 5.2 做性能收口**

---

## 五、说明

需要强调：

- 本轮工作并非无效，`benchmark.py` 和 `M7-T94-R` 已经提供了有价值的真实性能基线
- 失败点不在“是否建立了基线”，而在“当前基线数值不满足计划门槛”
- 因此下一轮不应重复“只测一次然后汇报基线”，而必须针对瓶颈做真实优化并重新跑到目标线以内

---

## 六、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T95-20260417-092530-p5-t52-performance-target-closure.md`

下一轮继续停留在：

1. Phase-5 / Task 5.2 再收口
2. 针对 5 个端点做真实性能优化
3. 重新执行 `scripts/benchmark.py`
4. 直到所有端点满足 P95 目标且无 `5xx` 为止
