# M7-T78 审计总结 — Phase-3 / Task 3.3 API 集成冒烟测试脚本核验

时间戳：20260416-153139  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T78-R-20260416-152000-p3-t33-api-smoke-test-script-report.md` 与 `scripts/smoke-test-api.py`  
审计结论：❌ **不通过（继续停留在 Task 3.3 收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T78-20260416-151003-p3-t33-api-smoke-test-script.md`
- `docs/tasks/M7/M7-T78-R-20260416-152000-p3-t33-api-smoke-test-script-report.md`
- `scripts/smoke-test-api.py`
- 任务单中的 `Task 3.3` 通过条件

---

## 二、通过项

### 1. 脚本已新增且可独立运行

当前已新增：

- `scripts/smoke-test-api.py`

并支持：

```bash
python scripts/smoke-test-api.py --api-url http://localhost:8000
```

脚本结构完整，参数入口、步骤输出、汇总输出与退出码逻辑都已具备。

### 2. API 覆盖范围达标

当前脚本覆盖 13 个步骤，满足任务单“覆盖 ≥ 10 个 API 端点”的要求，包括：

- 登录
- 资产扫描
- 上传
- 创建数据集
- 获取数据集
- 创建实验
- 提交训练
- 查看队列
- 轮询训练
- 获取结果
- 特征重要性
- 对比实验
- 登出

### 3. 每步 PASS/FAIL + 耗时输出已落地

从脚本实现与报告原始输出看，当前每一步都能输出：

- 步骤名
- `PASS` / `FAIL`
- 耗时

并且最终会输出汇总统计。

### 4. 退出码逻辑实现正确

脚本末尾已实现：

- 全部通过 → `sys.exit(0)`
- 存在失败 → `sys.exit(1)`

这与任务单要求一致。

---

## 三、不通过项

### 1. 真实执行结果未达到“脚本通过”状态

`M7-T78-R` 报告明确写明：

- 13 个步骤中 12 个通过
- 1 个失败
- 实际退出码为 `1`

而 `Task 3.3` 的目标不是“脚本存在且能失败得很诚实”，而是要完成一轮可作为验收依据的 API 冒烟闭环。

结论：

- 当前真实执行结果仍存在失败步骤
- 因此本轮不能按“Task 3.3 完成”验收通过

### 2. 总耗时不满足任务单硬性门槛

任务单通过条件明确要求：

- 总耗时 `< 120 秒（含训练等待）`

但本轮报告给出的真实结果为：

- `总耗时: 129.27s`

结论：

- 该硬性门槛未满足

### 3. 报告“建议提交验收”与通过条件不一致

`M7-T78-R` 中已如实列出：

- 退出码为 `1`
- 步骤 9 失败
- 总耗时 `129.27s`
- 通过条件检查中 `总耗时 < 120 秒` 仅为 `⚠️`

但结论部分仍写：

- “建议提交验收”

结论：

- 报告结论超前于任务单硬性通过条件

### 4. 环境限制虽真实存在，但当前仍构成验收阻断

报告说明步骤 9 失败的原因是：

- Worker 未运行，训练任务一直停留在 `queued`

这一点可以接受为**真实原因说明**，但并不能改变当前事实：

- 步骤 9 失败
- 退出码为 `1`
- 总耗时超限

结论：

- 这不是脚本实现造假问题
- 但在当前真实环境下，仍不足以让 `Task 3.3` 验收通过

---

## 四、审计结论

- **状态：不通过（Fail）**
- **主要原因：**
  1. 真实执行存在失败步骤
  2. 实际退出码为 `1`，非成功态
  3. 总耗时 `129.27s` 超出 `< 120s` 门槛
  4. 报告建议提交验收的结论超前

因此：

- **M7-T78 不予验收通过**
- **不得进入 Phase-4 或后续任务**
- **下一轮仍必须停留在 Task 3.3 收口范围内**

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T79-20260416-153139-p3-t33-worker-aware-api-smoke-closure.md`

下一轮必须聚焦：

1. 在 Worker 可运行的真实环境下完成完整链路闭环
2. 让脚本真实执行结果达到 exit code `0`
3. 将总耗时压到 `< 120s`
4. 若环境无法满足，也必须如实收口为“环境阻断，未通过”，不得包装成已完成
