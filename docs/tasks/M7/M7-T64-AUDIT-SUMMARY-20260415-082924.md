# M7-T64 审计总结 — Task 2.1 复核修复核验

时间戳：20260415-082924  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T64-R-20260414-163800-p2-t21-reopen-large-upload-proof-and-sync-open-cleanup-report.md`  
审计结论：❌ **不通过（需继续停留在 Task 2.1 范围内修复）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T64-20260414-152701-p2-t21-reopen-large-upload-proof-and-sync-open-cleanup.md`
- `docs/tasks/M7/M7-T64-R-20260414-163800-p2-t21-reopen-large-upload-proof-and-sync-open-cleanup-report.md`
- `apps/api/app/routers/datasets.py`
- `apps/api/tests/test_datasets.py`

---

## 二、通过项

### 1. `upload_file()` 中的同步行计数已清理

在 `apps/api/app/routers/datasets.py` 的 `upload_file()` 内：

- 原先的小文件同步计数 `sum(1 for _ in open(...))` 已删除
- 小文件和中文件分支都改为复用 `count_lines_async()`

这一点符合本轮任务对 `upload_file()` 的直接修复要求。

### 2. 已补出 `>50MB` 规模的测试样本

`apps/api/tests/test_datasets.py` 中：

- 现有测试名为 `test_upload_large_csv_over_50mb_does_not_block`
- 测试文件大小断言为 `>50MB`
- 报告中给出的实际样本大小为 `56.1MB`

这一点比上一轮 `10.5MB` 证据更接近任务口径。

### 3. 全量回归结果未出现 failure

报告给出的全量结果为：

- `354 passed, 9 skipped, 1 warning in 250.17s`

若该结果属实，则当前修改未引入新的失败用例。

---

## 三、不通过项

### 1. 核心阻断：没有实现任务单要求的 `assert elapsed < 30`

M7-T64 任务单明确要求：

- 测试必须包含显式断言 `assert elapsed < 30`

但在 `apps/api/tests/test_datasets.py` 的目标测试中，实际只看到：

- 记录 `start_time`
- 计算 `elapsed`
- `print(...)` 输出耗时

**没有**：

- `assert elapsed < 30`

结论：

- 本轮最关键的验收条件仍未满足。

### 2. 实际耗时证据直接失败：`elapsed=81.95s`

报告中给出的真实输出为：

- `Large file upload (>50MB): 56.1MB, elapsed=81.95s, row_count=1201494`

这意味着即使补上显式断言，当前实现/测试证据也会失败，因为：

- `81.95s > 30s`

结论：

- 当前不能宣称“非阻塞证据已提供并通过”。
- 当前最多只能说明“56.1MB 上传最终成功返回”，不能说明“满足任务定义的时延门槛”。

### 3. 报告存在不实勾选

报告“通过条件检查”中写的是：

- “测试对请求耗时有显式断言（300s 超时上限，考虑测试环境 IO 性能）”

但代码里并不存在针对 `elapsed` 的断言，只配置了：

- `Timeout(300.0, connect=10.0)`

这是**客户端超时配置**，不是**验收断言**。

结论：

- 报告把“超时配置”写成了“显式耗时断言”，属于事实不一致。

### 4. 报告擅自改写任务门槛

任务单要求的是：

- `assert elapsed < 30`

但报告和测试说明改成了：

- “120s 内完成”
- “300s 超时上限”
- “生产环境建议使用 30s 超时阈值”

结论：

- 这是未经批准擅自修改验收标准。
- 不能以环境性能为由把任务门槛从 `30s` 上调到 `120s/300s`。

### 5. “7/7 勾选通过”结论不成立

至少以下一项明确不成立：

- “测试对请求耗时有显式断言 `elapsed < 30`”

因此报告中的：

- “所有通过条件已验证（7/7 勾选）”

不成立。

---

## 四、补充观察

### 1. 路由文件中仍有其他同步 `open()`

虽然 `upload_file()` 已清理，但 `apps/api/app/routers/datasets.py` 其他位置仍存在：

- `preview_dataset()` 中 `sum(1 for _ in open(file_path)) - 1`

这**不属于本轮直接阻断项**，因为 M7-T64 只要求处理 `upload_file()`；但如果后续继续治理同类性能问题，可纳入后续任务。

### 2. 当前测试的主要瓶颈可能已转移到文件上传与请求内读取链路

从现有证据看：

- 这轮已经把“文件生成时间”移出计时范围
- 但请求阶段仍达到 `81.95s`

这说明瓶颈不再只是“测试文件生成慢”，还可能包括：

- 请求体上传方式
- API 端写入磁盘
- `pd.read_csv(..., nrows=100)` + 全文件异步计数组合成本
- ASGI 测试环境中的文件流行为

这部分需要下一轮以“定位 + 证据化”的方式继续收口，而不是直接改写门槛。

---

## 五、审计结论

- **状态：不通过（Fail）**
- **原因：**
  1. 未实现任务单强制要求的 `assert elapsed < 30`
  2. 真实证据显示 `elapsed=81.95s`，不满足 30s 门槛
  3. 报告把“超时配置”表述成“显式断言”
  4. 报告擅自把验收门槛从 30s 改写为 120s/300s

因此：

- **M7-T64 不予验收通过**
- **不得进入 Task 2.2（CORS 配置生产化）**
- 下一轮仍必须停留在 **Task 2.1 收口** 范围内

---

## 六、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T65-20260415-082924-p2-t21-latency-root-cause-and-threshold-compliance.md`

下一轮必须聚焦：

1. 让 `>50MB` 证据真正满足 `elapsed < 30`
2. 给出耗时拆分证据，证明瓶颈被定位且修复
3. 严格按任务门槛汇报，不得自改验收标准
