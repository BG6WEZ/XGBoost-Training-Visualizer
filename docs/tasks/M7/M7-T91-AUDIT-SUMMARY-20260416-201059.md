# M7-T91 审计总结 — Phase-5 / Task 5.1 Docker 全栈构建与启动验证核验

时间戳：20260416-201059  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T91-R-20260416-195700-p5-t51-docker-full-stack-validation-report.md` 与 `docker/docker-compose.yml`  
审计结论：❌ **不通过（继续停留在 Task 5.1 收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T91-20260416-190040-p5-t51-docker-full-stack-validation.md`
- `docs/tasks/M7/M7-T91-R-20260416-195700-p5-t51-docker-full-stack-validation-report.md`
- `docker/docker-compose.yml`
- `docs/planning/LAUNCH_DEVELOPMENT_PLAN.md` 中 `Task 5.1`

---

## 二、通过项

### 1. Docker 构建与基础启动已完成

当前报告已说明：

- 3 / 3 镜像构建成功
- 6 / 6 容器全部 `healthy/running`

这满足了本轮的构建与基础启动要求。

### 2. `/ready` 返回 200

当前报告已说明：

- `http://localhost:8000/ready` 返回 `200`
- 依赖检查正常

说明全栈基础依赖在 Docker 环境中已经拉起。

### 3. Compose 文件已做最小必要修正

当前 `docker/docker-compose.yml` 已补入：

- API 的 `JWT_SECRET`
- Worker 的 `JWT_SECRET`

这解释了为什么本轮能够进一步推进到容器级验证。

---

## 三、不通过项

### 1. API 冒烟测试未达到硬性通过条件

`Task 5.1` 的通过条件明确要求：

- `API 冒烟测试 exit code 0`

但当前报告给出的真实结果是：

- 12 / 13 passed
- 1 项失败：训练超时

结论：

- 当前 API 冒烟测试并未达到 `exit code 0`
- 因此未满足本轮硬门槛

### 2. 浏览器 E2E 通过率远低于 80%

`Task 5.1` 的通过条件明确要求：

- `浏览器 E2E 测试 ≥ 80% passed`

但当前报告给出的真实结果是：

- 17 / 20 失败
- 也就是仅 3 / 20 通过

通过率约为：

- `15%`

结论：

- 远低于 `80%` 门槛
- 当前无法视为浏览器层冒烟通过

### 3. 失败原因虽合理，但不能替代通过条件

报告说明：

- `VITE_API_URL=http://api:8000` 是 Docker 内部域名
- 浏览器从宿主机访问时无法解析

这能解释失败原因，但并不能改变当前事实：

- Playwright 冒烟没有达到通过阈值

同样地：

- 训练超时可解释为队列处理 / 超时窗口问题
- 但仍然意味着 API 冒烟不是成功态

结论：

- 当前是“失败原因已定位”，不是“Task 5.1 已通过”

---

## 四、审计结论

- **状态：不通过（Fail）**
- **主要原因：**
  1. API 冒烟测试未达到 `exit code 0`
  2. 浏览器 E2E 通过率仅约 `15%`，远低于 `80%`
  3. 失败原因虽已定位，但尚未完成收口修复

因此：

- **M7-T91 不予验收通过**
- **不得进入 Task 5.2**
- **下一轮仍必须停留在 Task 5.1 收口范围内**

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T92-20260416-201059-p5-t51-docker-host-browser-routing-and-smoke-closure.md`

下一轮必须聚焦：

1. 修正 Docker 场景下前端对 API 的宿主机可访问地址配置
2. 让 Playwright 冒烟达到至少 `80%` 通过
3. 让 API 冒烟达到 `exit code 0`
4. 完成后再重新提交 Task 5.1 验收
