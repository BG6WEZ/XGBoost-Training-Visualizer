# M7-T24 ~ M7-T27 阶段汇总

项目名称: XGBoost Training Visualizer  
汇总日期: 2026-04-07  
范围: M7-T24、M7-T25、M7-T26、M7-T27  
口径说明: 本文按“已审计通过 / 已提交汇报并完成最小验证 / 未验证”区分状态，不把“已写代码”或“已写汇报”写成“已审核通过”。

---

## 一、阶段状态总览

| 任务编号 | 任务名称 | 当前状态 | 已核验证据 |
|---------|---------|---------|-----------|
| M7-T24 | P1-T10 预测 vs 实际与残差分析 | 已在 M7-T25 审计修复后闭环 | 预测分析测试 5 passed；前端 typecheck/build 通过 |
| M7-T25 | M7-T24 审计修复 | 已审核通过 | 修复后预测分析测试 5 passed；M7-T24 报告已清理模板残留 |
| M7-T26 | P1-T11 Benchmark 模式与指标统一 | 已审核通过 | Benchmark 测试 22 passed；前端 typecheck/build 通过 |
| M7-T27 | P1-T12 实验标签与筛选 | 已提交汇报，已完成最小复核，待正式审核结论 | 标签与筛选测试 12 passed；前端 typecheck/build 通过 |

当前可确认结论：

1. M7-T24、M7-T25、M7-T26 已完成闭环，且已有明确审核结论。
2. M7-T27 目前不能直接写成“已审核通过”，因为本轮只完成了汇报读取和最小复核，还未形成完整单轮审计结论文档。
3. 截至当前，已复核的唯一功能测试总数为 39 条：`PredictionAnalysis` 5 条、`Benchmark` 22 条、`Tags/Filter` 12 条。

---

## 二、任务级总结

### 2.1 M7-T24: P1-T10 预测 vs 实际与残差分析

已确认能力：

1. 后端提供 `GET /api/results/{experiment_id}/prediction-analysis`。
2. 残差定义统一为 `residual = actual - predicted`。
3. 结果链路支持预测 vs 实际散点、残差分布、残差摘要与诚实降级语义。

闭环说明：

1. M7-T24 原始汇报不能直接判定通过。
2. 阻断点在 M7-T25 中被修复，随后以 M7-T25 的审核结论为准完成闭环。

已核验证据：

1. `python -m pytest tests/test_results.py -k "PredictionAnalysis" -v --tb=short`
2. 结果：5 passed。
3. 前端 typecheck/build 在闭环阶段通过。

### 2.2 M7-T25: M7-T24 审计修复

已确认修复项：

1. 修正了“无预测工件”场景的测试与证据闭环。
2. M7-T24 报告去除了模板残留，改成单一正式报告。
3. 明确补齐了真实命令与真实输出，不再用结论代替证据。

已核验证据：

1. `python -m pytest tests/test_results.py -k "PredictionAnalysis" -v --tb=short`
2. 结果：5 passed in 1.39s。
3. 前端 `npx tsc --noEmit`、`pnpm build` 通过。

### 2.3 M7-T26: P1-T11 Benchmark 模式与指标统一

已确认能力：

1. 后端定义了统一 `BenchmarkMetrics` 结构。
2. 已接入 `RMSE`、`MAE`、`MAPE`、`R2` 四个指标及可用性说明。
3. 不可计算场景采用诚实降级，不伪造默认值。

已核验证据：

1. `python -m pytest tests/test_benchmark.py -v --tb=short`
2. 结果：22 passed。
3. 前端 `npx tsc --noEmit`、`pnpm build` 通过。

### 2.4 M7-T27: P1-T12 实验标签与筛选

当前已确认内容：

1. M7-T27 任务单与汇报文件均已存在。
2. 标签与筛选相关测试文件存在，且本轮重新执行通过。
3. 前端最小门禁本轮重新执行通过。

本轮已做最小复核：

1. 读取 M7-T27 汇报。
2. 重新执行 `python -m pytest tests/test_tags.py -v --tb=short`。
3. 结果：12 passed in 2.37s。
4. 重新执行前端 `npx tsc --noEmit` 与 `pnpm build`，均通过。

当前仍保留的审计边界：

1. 尚未像 M7-T25、M7-T26 那样完成一次完整“任务要求对照代码实现”的逐项正式审核。
2. 因此本文只把 M7-T27 记为“已提交汇报并完成最小复核”，不直接记为“已审核通过”。

---

## 三、关键改动面

后端与测试侧已确认涉及：

1. `apps/api/app/routers/results.py`
2. `apps/api/app/schemas/results.py`
3. `apps/api/app/services/benchmark.py`
4. `apps/api/app/routers/experiments.py`
5. `apps/api/app/schemas/experiment.py`
6. `apps/api/app/models/models.py`
7. `apps/api/tests/test_results.py`
8. `apps/api/tests/test_benchmark.py`
9. `apps/api/tests/test_tags.py`
10. `apps/worker/app/tasks/training.py`

前端侧已确认涉及：

1. `apps/web/src/lib/api.ts`
2. `apps/web/src/components/PredictionAnalysis.tsx`

说明：

1. 上述列表表示本阶段已被报告、搜索或验证链路覆盖到的关键文件。
2. 本文不把“报告中声称修改”自动等同为“已逐文件完成人工审核”。

---

## 四、已验证边界

已验证：

1. 预测分析后端 focused 测试：5 passed。
2. Benchmark focused 测试：22 passed。
3. 标签与筛选 focused 测试：12 passed。
4. 前端 typecheck：通过。
5. 前端生产构建：通过。

未在本汇总中宣称已验证：

1. 完整运行态端到端 UI 验证。
2. Worker 驱动的真实训练链路回放。
3. 全量 API 回归在当前阶段持续绿灯。
4. M7-T27 的完整逐项正式审核结论。

---

## 五、风险与限制

1. 历史实验若缺少逐样本预测工件，预测分析与 Benchmark 只能诚实降级，不能补算历史结果。
2. MAPE 对真实值为 0 的样本敏感，属于设计内降级，不应被误判为 bug。
3. 标签筛选当前已通过 focused 测试，但若后续数据量显著增大，需要继续关注筛选性能和后端实现口径。
4. 前端 build 当前仅有 chunk size warning，不影响通过结论，但后续可以作为性能优化项处理。

---

## 六、相关报告文件

1. `docs/tasks/M7/M7-T24-R-20260407-085126-p1-t10-prediction-vs-actual-and-residual-analysis-report.md`
2. `docs/tasks/M7/M7-T25-R-20260407-092723-m7-t24-audit-fixes-backend-degradation-and-evidence-closure-report.md`
3. `docs/tasks/M7/M7-T26-R-20260407-094029-p1-t11-benchmark-mode-and-metrics-unification-report.md`
4. `docs/tasks/M7/M7-T27-R-20260407-102026-p1-t12-experiment-tags-and-filtering-report.md`

---

## 七、结论

截至 2026-04-07，M7-T24 ~ M7-T27 的真实阶段结论是：

1. P1-T10 已通过修复轮完成闭环。
2. P1-T11 已审核通过。
3. P1-T12 已有实现、汇报和最小验证，但仍建议在进入下一任务前先完成一次正式审核结论。

如果后续继续沿用当前工作流，下一步应以 M7-T27 汇报为输入，完成正式审核，并基于审核结果决定生成下一轮任务单还是先下发审计修复单。
