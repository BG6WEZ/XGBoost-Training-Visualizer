# M7-T74 审计总结 — Phase-3 / Task 3.2 前端 E2E 测试（Playwright）补收口核验

时间戳：20260416-095303  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T73-R-20260416-084000-p3-t32-frontend-e2e-playwright-report.md` 与当前 `apps/web/e2e/` 实际状态  
审计结论：❌ **不通过（继续停留在 Task 3.2 收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T74-20260416-085339-p3-t32-playwright-execution-and-flow-hardening.md`
- `docs/tasks/M7/M7-T73-R-20260416-084000-p3-t32-frontend-e2e-playwright-report.md`
- `apps/web/package.json`
- `apps/web/playwright.config.ts`
- `apps/web/e2e/auth.spec.ts`
- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`
- `apps/web/e2e/screens/` 实际截图目录

---

## 二、通过项

### 1. Playwright 基础执行环境已可用

当前仓库已满足以下基础条件：

- `apps/web/package.json` 中存在 `test:e2e` 脚本
- `apps/web/playwright.config.ts` 已将 `baseURL` 与 `webServer` 统一到端口 `3000`
- `apps/web/e2e/screens/` 已实际存在，并落盘 19 张截图文件

### 2. 用例数量满足最低门槛

当前 E2E 目录下已有 4 个测试文件，名义用例数 20 个，满足“至少 10 个 E2E 用例”的最低数量要求。

---

## 三、不通过项

### 1. 缺少本轮任务单要求的 `M7-T74-R` 完成报告

`M7-T74` 明确要求产出：

- `M7-T74-R-<timestamp>-p3-t32-playwright-execution-and-flow-hardening-report.md`

但当前 `docs/tasks/M7/` 下并不存在任何 `M7-T74-R-*.md` 文件。

结论：

- 本轮缺少与任务编号一致的正式收口报告
- 不能用旧的 `M7-T73-R` 报告替代 `M7-T74-R`

### 2. 仍然存在静默跳过式通过逻辑

`M7-T74` 已明确禁止以下模式继续作为“通过”逻辑：

- `if (await locator.isVisible(...).catch(() => false)) { ... }`

当前仍可在测试代码中实际检出：

- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`

这意味着一旦关键元素不存在，测试仍可能绕过关键动作后继续通过。

结论：

- “不存在静默跳过式通过逻辑”这一通过条件未满足

### 3. 多个核心流程依旧停留在“页面可见/直接跳转”层面

`M7-T74` 要求把弱用例补强为“真实动作 + 真实断言”的业务流程验证，但当前仍存在以下问题：

- `assets.spec.ts` 的上传流程在按钮或文件输入不存在时不会失败，只会截图后结束
- `assets.spec.ts` 未落实“上传 CSV -> 注册数据集 -> 跳转详情并验证详情内容”的完整主路径
- `experiments.spec.ts` 仍主要验证“按钮可见”“表单出现”“列表或表格可能存在”
- `results.spec.ts` 仍包含直接 `goto()` 到结果相关 URL 后只断言页面可见的用例
- `results.spec.ts` 的实验详情用例在找不到实验链接时不会失败

结论：

- 当前仍有多项用例不是任务单要求的真实业务 E2E

### 4. 报告内容与当前任务约束不完全一致

当前唯一存在的报告文件仍为：

- `docs/tasks/M7/M7-T73-R-20260416-084000-p3-t32-frontend-e2e-playwright-report.md`

该报告虽然写明“20/20 passed”，但就 `M7-T74` 的验收要求而言，仍缺少：

- 与 `T74` 对应的正式报告编号
- 本轮要求的完整收口结构
- 对“弱用例已补强为真实业务流程”的逐项自证

结论：

- 当前文档状态不足以支撑 `T74` 验收通过

---

## 四、审计结论

- **状态：不通过（Fail）**
- **主要原因：**
  1. 缺少 `M7-T74-R` 正式完成报告
  2. 仍存在静默跳过式测试逻辑
  3. 关键业务流程尚未全面落实为“真实动作 + 真实断言”
  4. 当前报告与本轮任务编号、收口要求不一致

因此：

- **M7-T74 不予验收通过**
- **不得进入 Task 3.3**
- **下一轮仍必须停留在 Task 3.2 收口范围内**

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T75-20260416-095303-p3-t32-real-business-e2e-closure.md`

下一轮必须聚焦：

1. 去掉所有静默跳过式通过逻辑
2. 把资产 / 实验 / 结果相关弱用例补强为真实业务主路径
3. 以 `M7-T75-R-...` 形式提交与本轮任务编号一致的正式报告
4. 在报告中诚实区分“已验证通过”和“未验证/无法验证”
