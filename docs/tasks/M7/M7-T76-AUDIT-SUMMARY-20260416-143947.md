# M7-T76 审计总结 — Phase-3 / Task 3.2 再收口（实验/结果真实主路径闭环）核验

时间戳：20260416-143947  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T76-R-20260416-143500-p3-t32-experiments-results-real-flow-closure-report.md` 与当前 `apps/web/e2e/` 实际状态  
审计结论：❌ **不通过（继续停留在 Task 3.2 收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T76-20260416-112613-p3-t32-experiments-results-real-flow-closure.md`
- `docs/tasks/M7/M7-T76-R-20260416-143500-p3-t32-experiments-results-real-flow-closure-report.md`
- `apps/web/package.json`
- `apps/web/playwright.config.ts`
- `apps/web/e2e/auth.spec.ts`
- `apps/web/e2e/assets.spec.ts`
- `apps/web/e2e/experiments.spec.ts`
- `apps/web/e2e/results.spec.ts`
- `apps/web/e2e/screens/` 实际截图目录

---

## 二、通过项

### 1. `experiments.spec.ts` 与 `results.spec.ts` 的静默跳过逻辑已基本清除

相较上一轮，以下关键问题已明显改善：

- `experiments.spec.ts` 已改为 `beforeAll` 通过 API 创建数据集与实验
- `results.spec.ts` 已改为 `beforeAll` 通过 API 创建数据集与实验
- 当前未再检出上一轮那种 `catch(() => false)` 形式的实验/结果关键路径跳过逻辑

说明本轮在实验/结果测试结构上有实质前进。

### 2. 实验与结果流程开始绑定真实 ID / 真实数据准备

当前代码已体现：

- 通过 API 创建真实 `datasetId`
- 通过 API 创建真实 `experimentId`
- 结果页与详情页已开始使用真实 ID 导航

这比上一轮“直接打开固定页面 URL”更接近真实业务主路径。

---

## 三、不通过项

### 1. 截图证据与报告仍不一致

`M7-T76-R` 报告写明：

- “All 20 screenshots collected”
- 报告列出 `TC8-upload-success.png`

但当前 `apps/web/e2e/screens/` 实际核验仅见 18 张截图，并缺少：

- `TC4-*`
- `TC8-*`

同时报告列出的：

- `TC8-upload-success.png`

在实际目录中并不存在。

结论：

- “截图证据实际生成并与报告逐项一致”这一通过条件未满足

### 2. 结果流程仍未达到任务单要求的“真实结果页断言 + 对比状态变化断言”

`M7-T76` 的通过条件明确要求：

- 结果流程达到“真实结果页断言 + 对比状态变化断言”

但当前 `results.spec.ts` 实际仅做到：

- 打开 `/compare`
- 断言标题可见
- 打开 `/assets/{datasetId}/quality`
- 断言 `body` 可见
- 打开 `/experiments/{experimentId}`
- 断言页面包含实验名

可见 [results.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/results.spec.ts#L121-L178)

其中仍缺失：

- 多实验选择后的对比状态变化断言
- 真实结果指标/图表区块断言

而报告自身也承认未验证真实对比结果与真实训练结果，可见 [M7-T76-R](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/docs/tasks/M7/M7-T76-R-20260416-143500-p3-t32-experiments-results-real-flow-closure-report.md#L154-L160)

结论：

- 结果主路径通过条件未满足

### 3. 报告仍把未满足的通过条件描述成“全部问题已解决”

`M7-T76-R` 结论写明：

- “All M7-T75 audit issues have been resolved.”

但当前仍存在：

- 截图证据与报告不一致
- 结果流程未达到本轮任务单要求的真实对比状态变化断言

结论：

- 报告对完成度的表述仍然超前

### 4. 全量“零静默跳过”表述仍不严谨

虽然 `experiments.spec.ts` / `results.spec.ts` 的关键问题已显著改善，但当前全量 E2E 代码中仍存在条件分支式逻辑，例如：

- [auth.spec.ts](file:///c:/Users/wangd/project/XGBoost%20Training%20Visualizer/apps/web/e2e/auth.spec.ts#L105-L125)

这里不是上一轮那种 `catch(() => false)` 模式，但依然属于“若满足条件则执行额外步骤”的写法。

结论：

- 若报告要写“across all test files zero silent skip patterns”，需要更谨慎，避免超出当前事实

---

## 四、审计结论

- **状态：不通过（Fail）**
- **主要原因：**
  1. 截图证据与报告仍不一致
  2. 结果流程尚未满足“真实结果页断言 + 对比状态变化断言”
  3. 报告对完成度的表述仍超出当前代码与验证事实

因此：

- **M7-T76 不予验收通过**
- **不得进入 Task 3.3**
- **下一轮仍必须停留在 Task 3.2 收口范围内**

---

## 五、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T77-20260416-143947-p3-t32-results-assertion-and-evidence-alignment.md`

下一轮必须聚焦：

1. 补齐实际缺失的截图或如实修改报告
2. 让结果流程落实至少一个真实结果断言与一个真实对比状态变化断言
3. 收紧全量报告措辞，避免再写“全部问题已解决”式结论
4. 完成后再重新提交 Task 3.2 验收
