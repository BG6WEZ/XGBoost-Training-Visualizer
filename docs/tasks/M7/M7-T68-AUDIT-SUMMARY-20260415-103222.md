# M7-T68 审计总结 — Task 2.3 Docker Compose 清理核验

时间戳：20260415-103222  
审计员：GPT-5.4  
审计对象：`docs/tasks/M7/M7-T68-R-20260415-102700-p2-t23-docker-compose-cleanup-report.md`  
审计结论：❌ **不通过（需继续停留在 Task 2.3 范围内收口）**

---

## 一、核验材料

- `docs/tasks/M7/M7-T68-20260415-101208-p2-t23-docker-compose-cleanup.md`
- `docs/tasks/M7/M7-T68-R-20260415-102700-p2-t23-docker-compose-cleanup-report.md`
- `docker/docker-compose.yml`
- `docker/docker-compose.prod.yml`
- `docker/docker-compose.dev.yml`
- `docker/.env.example`
- 实际复跑：
  - `docker compose -f docker/docker-compose.yml config`
  - `docker compose -f docker/docker-compose.dev.yml config`
  - `docker compose -f docker/docker-compose.prod.yml --env-file docker/.env.example config`

---

## 二、通过项

### 1. 三个 compose 文件的 `version:` 已移除

当前三套 compose 文件均未再出现顶层 `version:` 字段，这一点符合任务要求。

### 2. `prod` compose 已加入重启策略

`docker/docker-compose.prod.yml` 中长期运行服务已添加：

- `restart: unless-stopped`

这一点符合任务要求。

### 3. `prod` compose 中敏感值已改为环境变量引用

当前 `docker/docker-compose.prod.yml` 中，数据库、MinIO 等敏感配置均已改为 `${VAR_NAME}` 形式，不再看到直接明文硬编码密码/密钥。

### 4. `docker/.env.example` 已重写为占位模板

`docker/.env.example` 已提供较完整的变量占位清单，且未写入真实密钥。

### 5. 三条 `docker compose config` 命令都可执行

我实际复跑后：

- 三条命令均退出码 `0`
- `docker-compose.dev.yml` 与 `docker-compose.prod.yml` 可正常解析

---

## 三、不通过项

### 1. 核心阻断：`docker/docker-compose.yml config` 仍然产生大量 warning

M7-T68 任务单明确要求：

- “无 Compose 配置 warning”
- “不出现 `version is obsolete` 或同类 warning”

但我实际执行：

- `docker compose -f docker/docker-compose.yml config`

输出中仍出现多条警告，例如：

- `The "DATABASE_URL" variable is not set. Defaulting to a blank string.`
- `The "REDIS_URL" variable is not set. Defaulting to a blank string.`
- `The "MINIO_ENDPOINT" variable is not set. Defaulting to a blank string.`
- `The "POSTGRES_USER" variable is not set. Defaulting to a blank string.`

结论：

- 当前不能声称“三个 compose 文件均无 warning”。
- 本轮最关键的验收项之一尚未完成。

### 2. 报告结论与实际复跑结果不一致

报告中写的是：

- “三个 compose 文件均已符合现代 Compose 规范，无 `version is obsolete` warning”

但实际复跑显示：

- 虽然确实没有 `version is obsolete`
- 但 `docker-compose.yml` 仍有大量“变量未设置”的 warning

结论：

- 报告把“没有 `version` 警告”扩大表述为“没有 Compose warning”，不成立。

### 3. 基础 compose 的变量收口未闭环

从当前 `docker/docker-compose.yml` 看：

- API / Worker / Postgres / MinIO 使用了多个 `${VAR}` 引用
- 但任务要求的验证命令并未为该文件提供 `--env-file`

这意味着：

- 只要直接执行 `docker compose -f docker/docker-compose.yml config`
- 就会继续出现空变量 warning

结论：

- 当前配置与验证方式之间还没有闭环，需要二选一修正：
  1. 让 `docker-compose.yml` 在无额外 env-file 时也不触发 warning；或
  2. 明确补齐与其匹配的 env 模板和验证方式，并同步修订任务链

在当前任务单口径下，本轮尚未完成。

---

## 四、补充观察

### 1. `docker-compose.dev.yml` 仍保留开发态默认凭据

例如：

- `POSTGRES_PASSWORD=xgboost123`
- `MINIO_ROOT_PASSWORD=minioadmin`

这不构成本轮直接阻断，因为任务单只要求清理 `prod` 的硬编码凭据；但后续若要进一步收敛开发环境配置，可单列任务处理。

### 2. `prod` 健康检查仍使用固定数据库名/用户

`docker/docker-compose.prod.yml` 中 `postgres` 健康检查仍为：

- `pg_isready -U xgboost -d xgboost_vis`

如果未来 `POSTGRES_USER` / `POSTGRES_DB` 改为非默认值，该健康检查可能与环境变量不一致。  
这不是本轮任务单的明确阻断项，但建议在后续顺手修正。

---

## 五、审计结论

- **状态：不通过（Fail）**
- **原因：**
  1. `docker-compose.yml config` 实际仍产生多条 warning
  2. “无 Compose warning”的通过条件未满足
  3. 报告结论与实际复跑结果不一致

因此：

- **M7-T68 不予验收通过**
- **不得进入 Task 2.4**
- 下一轮仍必须停留在 **Task 2.3 收口** 范围内

---

## 六、下一步

已生成下一轮任务单：

- `docs/tasks/M7/M7-T69-20260415-103222-p2-t23-compose-warning-closure.md`

下一轮必须聚焦：

1. 让 `docker-compose.yml config` 不再产生变量未设置 warning
2. 如实重跑三条 compose 验证命令并附原始输出
3. 重新提交不夸大结论的总结单
