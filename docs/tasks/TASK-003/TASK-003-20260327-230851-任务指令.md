# TASK-003 任务指令（2026-03-27）

## 零、开始前必须先做

1. 先阅读并遵守以下规则文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_WORK_RULES.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_REPORT_TEMPLATE.md
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

2. 再阅读你上一轮汇报文件：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/reports/2026-03-28-task-002-browser-e2e-workspace-governance.md

3. 先回复你本轮将调用哪些内部智能体、各自负责什么，再开始执行。

4. 最终仍由你统一汇报，不能只给摘要，必须给证据。

---

## 一、任务信息

- 任务编号：TASK-003
- 下发时间：2026-03-27
- 生效时间：立即
- 执行窗口：2026-03-27 至 2026-03-28
- 本轮停点：完成本任务单中的 2 个任务后必须暂停，等待人工验收

---

## 二、审查结论（本轮输入）

基于对最新汇报和仓库现状的审查，本轮结论如下：

1. 浏览器 E2E 证据文件已存在（screenshot-*.png）。
2. workspace 仍存在多入口风险：
- 项目根目录存在 workspace/splits、workspace/preprocessing、workspace/models
- apps/api/workspace 与 apps/worker/workspace 也存在子目录
3. 上一轮已给出治理方案，但尚未落地为“代码级统一 + 自动化校验”。

因此本轮目标：从“方案”进入“落地修复 + 可回归验证”。

---

## 三、Trae 多智能体协作（建议）

- system-architect：确定统一 WORKSPACE_DIR 方案与迁移策略
- backend-expert：落地 API 侧路径统一与回溯接口/逻辑校验
- ai-ml-engineer：评估训练产物路径变更对训练流程影响
- data-engineer：校验 splits/preprocessing 产物迁移一致性
- qa-engineer：新增回归测试与路径一致性检查脚本
- devops-architect：补充环境变量、启动脚本与运行验证命令
- tech-lead-architect：收口审查、风险分级、最终汇报

说明：可自行增减智能体，但必须在汇报中写明分工与证据。

---

## 四、任务范围

### 任务 1：统一 workspace 路径并完成迁移验证（必须完成）

目标：
- API 与 Worker 统一使用“同一绝对 WORKSPACE_DIR”。

必须完成：
1. 代码与配置统一：
- API、Worker、脚本中所有 workspace 默认路径对齐到项目根 workspace
- 禁止再出现相对路径导致的漂移（如 ./workspace 指向不同 cwd）

2. 迁移与兼容：
- 对已有产物给出迁移动作（或兼容读取策略）
- 不允许破坏现有实验结果读取能力

3. 实际验证：
- 至少执行 1 次完整链路（切分 -> 训练 -> 结果读取）
- 提供路径证据，证明产物写入同一 workspace 根目录

验收标准：
- 代码层统一完成
- 运行链路验证通过
- 无新增路径漂移风险

---

### 任务 2：建立 workspace 一致性守卫（必须完成）

目标：
- 防止未来再次出现多 workspace 漂移。

必须完成：
1. 新增最小自动化检查（任选其一或组合）：
- 单元测试/集成测试：断言 API 与 Worker 的 WORKSPACE_DIR 解析一致
- 启动自检脚本：输出并比较关键服务的 workspace 绝对路径，不一致即失败

2. 文档同步：
- 更新 README 或相关运行文档中的 workspace 说明
- 明确“单一 workspace 根目录”原则

3. 验证结果分级：
- 必须区分：通过 / 跳过 / 未运行 / 失败

验收标准：
- 守卫机制可执行
- 文档与实现一致
- 回归可重复

---

## 五、范围限制（强约束）

- 只允许执行 TASK-003 中定义的 2 个任务。
- 不得提前开发下一阶段功能。
- 不得将“未验证”写成“已验证通过”。
- 不得以口头结论替代命令输出和文件证据。

---

## 六、交付物要求

1. 阶段汇报文档（必须落盘）：
- 路径：docs/reports/
- 文件名：2026-03-28-task-003-workspace-unification-guard.md

2. 汇报必须包含：
- 已完成任务编号
- 修改文件清单（逐项目的）
- 实际执行命令
- 实际结果与关键证据
- 已验证/未验证清单
- 风险与限制
- 是否建议继续下一任务

3. 提交前必须附并勾选：
- C:/Users/wangd/project/XGBoost Training Visualizer/docs/collaboraion/CLAUDE_ACCEPTANCE_CHECKLIST.md

---

## 七、停点要求

- 本轮完成后必须暂停。
- 等待人工验收与下一轮指令。
